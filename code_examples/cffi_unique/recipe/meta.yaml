package:
  name: make_unique
  version: 0.1

build:
  number: 0

source:
  path: ../

requirements:
  build:
    - python
    - setuptools
    - cmake

  run:

about:
  summary: An example project built with pybind11.
  license_file: LICENSE
